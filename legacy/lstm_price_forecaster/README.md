# Legacy LSTM Price Forecaster

This directory contains the original interactive LSTM price-prediction workflow. It is retained only as historical context and is not part of the main research pipeline.

The active project is the walk-forward ETF tactical allocation backtester under `src/quant_portfolio`, driven by `scripts/run_backtest.py`.
